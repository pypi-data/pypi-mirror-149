from setuptools import setup, find_packages
import os

setup(
    name='mkdocs-khneu',
    version='1.1',
    author='Department of e-learning tools',
    packages=find_packages(),
)
os.mkdir('test')